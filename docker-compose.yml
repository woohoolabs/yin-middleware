version: '2'

services:
    yin-middleware-php:
        container_name: yin-middleware-php
        image: php:7.4-cli
        volumes:
            - .:/var/www
        command: /bin/sh -c "cd /var/www; php vendor/bin/phpunit"
